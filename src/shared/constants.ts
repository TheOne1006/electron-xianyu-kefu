/**
 * 共享常量定义
 *
 * 跨进程（main / renderer / preload / injected）共用的常量统一在此定义。
 */

// ============================================================
// Product 相关常量
// ============================================================

/** 主图 URL 比较长度（前 N 个字符相同视为同一商品） */
export const PRODUCT_MAIN_IMAGE_URL_COMPARE_LENGTH = 72
